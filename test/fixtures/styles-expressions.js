const expr = 'test'

export default `${expr} ${expr} ${expr},
  div {
  display: ${expr};
    color: ${expr};
    ${expr};
    ${expr}
    ${expr};
    background: red;
  animation: ${expr} 10s ease-out;
  }

   div {
  color: red;
${expr}
}

div {
  color: red;
${expr};
}

  @media (${expr}) {

${expr}
   span.${expr} {
    color: red;
   }

${expr}

  ${expr} {
    color: red;
  }
    ${expr}, ${expr} {
      color: red;
    }

   ${expr}

    div, ${expr} {
      color: red;
    }
  }


  @media (min-width: ${expr}) {
   div.${expr} {
    color: red;
   }
  all${expr} {
${expr}
    color: red;
  }
  }

  @font-face {
    ${expr}
}`
